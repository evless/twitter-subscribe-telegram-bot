{
  "name": "twitter-subscribe-telegram-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:prod": "run-s clean compile docker:build docker:push",
    "build:dev": "run-s clean compile start",
    "clean": "run-p clean:*",
    "clean:build": "rm -rf build/",
    "compile": "babel app -d build",
    "start": "node build/index.js",
    "docker:build": "sudo docker build -f ./Dockerfile -t $npm_package_dockerRegistry/$npm_package_name:$npm_package_version .",
    "docker:push": "sudo docker push $npm_package_dockerRegistry/$npm_package_name:$npm_package_version"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-stage-0": "^6.24.1",
    "npm-run-all": "^4.0.2"
  },
  "dependencies": {
    "config": "^1.28.1",
    "cron": "^1.3.0",
    "js-yaml": "^3.10.0",
    "moment": "^2.19.1",
    "oauth": "^0.9.15",
    "pm2": "^2.8.0",
    "telegraf": "^3.15.3"
  },
  "dockerRegistry": "evless"
}
